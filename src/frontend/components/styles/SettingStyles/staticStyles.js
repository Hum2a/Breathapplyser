import { StyleSheet } from 'react-native';

export const StaticVariablesStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#E0F2F1', // Light blue background color
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
